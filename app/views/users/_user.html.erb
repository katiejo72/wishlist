<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.fname + ' ' + user.lname, user %>
  <% if current_user.admin? && !current_user?(user) %>
  	| <%= link_to "exterminate", user, method: :delete, data: { confirm: "For reals?" } %>
  <% end %>
</li>