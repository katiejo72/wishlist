<% provide(:title, "Sign Up") %>
<h1>Sign Up</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@user) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Allons-y!", class: "btn btn-medium btn-success" %>
		<% end %>
	</div>
</div>
